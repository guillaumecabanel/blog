<% content_for :meta_title, "Tous les articles" %>

<h2 class="mt-16 text-2xl md:text-3xl lg:text-4xl lg:leading-tight font-bold text-gray-800 text-center">
  Blog
</h2>
<p class="text-lg lg:text-xl font-medium text-gray-600 text-center">Tous les articles</p>

<div class="lg:px-32 xl:px-64 mt-16">
  <% @articles.each_with_index do |article, index| %>
    <%= render article, index: index %>
  <% end %>
</div>
